# Runtime dependencies for the whatsapp integration.
#
# The WhatsApp Cloud API (Meta Graph API) is hit via raw HTTP (httpx).
# httpx and langchain-core are in core deps; nothing extra needed.

dependencies = []
