# Runtime dependencies for the zerobounce integration.
#
# The ZeroBounce v2 REST API is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra needed.

dependencies = []
