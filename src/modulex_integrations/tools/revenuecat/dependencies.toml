# Runtime dependencies for the revenuecat integration.
#
# The RevenueCat REST API v1 is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra needed.

dependencies = []
