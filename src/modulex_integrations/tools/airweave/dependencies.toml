# Runtime dependencies for the airweave integration.
#
# The Airweave Search REST API is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra needed.

dependencies = []
