# Runtime dependencies for the weaviate integration.
#
# The Weaviate GraphQL + REST APIs are hit via raw HTTP (httpx). httpx
# and langchain-core are in core deps; nothing extra needed.

dependencies = []
