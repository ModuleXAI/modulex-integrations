# Runtime dependencies for the greenhouse integration.
#
# The Greenhouse Harvest API is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra needed.

dependencies = []
