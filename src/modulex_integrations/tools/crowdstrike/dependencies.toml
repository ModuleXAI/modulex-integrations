# Runtime dependencies for the crowdstrike integration.
#
# The CrowdStrike Falcon API is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra is needed.

dependencies = []
