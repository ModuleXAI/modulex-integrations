# Runtime dependencies for the neverbounce integration.
#
# The NeverBounce v4 REST API is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra needed.

dependencies = []
