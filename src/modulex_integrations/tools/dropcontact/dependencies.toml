# Runtime dependencies for the dropcontact integration.
#
# The Dropcontact REST API is hit via raw HTTP (httpx). httpx and
# langchain-core are in core deps; nothing extra needed.

dependencies = []
